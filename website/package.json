{
  "name": "pointee-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4180 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4180 --strictPort",
    "check": "node --check src/main.js && node --test test/*.test.mjs"
  },
  "dependencies": {
    "motion": "13.2.0"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "vite": "8.2.2"
  }
}
